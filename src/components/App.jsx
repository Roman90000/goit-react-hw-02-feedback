import { Layout } from '../Layout';
import 'modern-normalize';
import { Feedback } from './Feedback/Feedback';

export const App = () => {
  return (
    <Layout>
      <Feedback />
    </Layout>
  );
};
